{
  "name": "hrms-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader runner",
    "build": "node scripts/check-styles.mjs && vite build --configLoader runner",
    "check:styles": "node scripts/check-styles.mjs",
    "preview": "vite preview --port 3000"
  },
  "dependencies": {
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "5.0.4",
    "vite": "6.4.2"
  }
}
